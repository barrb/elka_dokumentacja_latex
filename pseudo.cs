/*
InputBox with value validation - first define validation delegate, which
returns empty string for valid values and error message for invalid values
*/
function nowa()
	b = Array[]
	a = new [123];
	
	for(i in a)
		a[i]=1234;

	let g := c
	for i in  b do %doing sth
		nowa();
	w = "dddddd"
	zz = 'dddddd'
	
